# PostgreSQL — URL de conexión
DATABASE_URL=postgresql://postgres:tu_contraseña@localhost:5432/egresados_db

# JWT — genera uno seguro: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
JWT_SECRET=cambia_esto_por_un_secreto_seguro_de_32_caracteres

# Seed (opcional)
ADMIN_EMAIL=admin@estadistica.bo
ADMIN_PASSWORD=Admin1234!
