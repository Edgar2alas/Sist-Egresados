// src/app/(egresado)/layout.tsx
import PublicLayout from "@/components/shared/PublicLayout";

export default function EgresadoLayout({ children }: { children: React.ReactNode }) {
  return <PublicLayout>{children}</PublicLayout>;
}
