src/
├── app/
│   ├── page.tsx                          # Redirige según rol (admin→dashboard, egresado→mi-perfil)
│   ├── layout.tsx                        # Layout raíz, metadatos globales
│   ├── globals.css                       # Estilos globales, variables CSS institucionales, clases utilitarias
│   ├── not-found.tsx                     # Página 404
│   │
│   ├── login/
│   │   ├── page.tsx                      # Envuelve en PublicLayout
│   │   └── LandingLoginPage.tsx          # Landing pública + modal de login
│   │
│   ├── activar-cuenta/page.tsx           # Flujo de primer login: código 6 dígitos → nueva contraseña
│   ├── recuperar-password/page.tsx       # Flujo 3 pasos: correo → código → nueva contraseña
│   ├── registro-inicial/page.tsx         # Egresado sin perfil completa sus datos por primera vez
│   ├── directorio/page.tsx               # Directorio público de egresados (server component con filtros)
│   │
│   ├── dashboard/page.tsx                # Panel admin: estadísticas generales del sistema
│   ├── egresados/
│   │   ├── page.tsx                      # Listado con filtros avanzados y paginación
│   │   ├── nuevo/page.tsx                # Formulario crear egresado
│   │   └── [id]/
│   │       ├── page.tsx                  # Detalle de egresado: datos, historial, postgrados
│   │       └── editar/page.tsx           # Editar datos de egresado
│   │
│   ├── reportes/page.tsx                 # Filtros + tabla + gráficos + exportar Excel
│   ├── usuarios/
│   │   ├── page.tsx                      # Listado de usuarios del sistema
│   │   ├── nuevo/page.tsx                # Crear usuario
│   │   └── [id]/editar/page.tsx          # Editar usuario
│   ├── verificaciones/page.tsx           # Admin revisa documentos de experiencia laboral
│   │
│   ├── (egresado)/                       # Grupo de rutas para rol egresado (usa PublicLayout)
│   │   ├── layout.tsx
│   │   ├── mi-perfil/page.tsx            # Ver perfil, historial, postgrados, toggle directorio
│   │   ├── editar-perfil/page.tsx        # Editar datos personales y académicos
│   │   └── experiencia/nueva/page.tsx    # Agregar nueva experiencia laboral
│   │
│   └── api/
│       ├── auth/
│       │   ├── login/route.ts            # POST: valida credenciales, crea sesión JWT
│       │   ├── logout/route.ts           # POST: elimina cookie de sesión
│       │   ├── link-egresado/route.ts    # POST: vincula idEgresado al token de sesión
│       │   ├── solicitar-codigo/route.ts # POST: genera y envía código 6 dígitos por correo
│       │   └── cambiar-password/route.ts # POST: valida código y actualiza contraseña
│       ├── egresados/
│       │   ├── route.ts                  # GET (listado paginado+filtros) / POST (crear)
│       │   ├── [id]/route.ts             # GET / PUT / DELETE por id
│       │   ├── destacados/route.ts       
│       │   └── directorio/route.ts       # POST: toggle mostrar_en_directorio del egresado
│       ├── historial/
│       │   ├── route.ts                  # GET (por egresado) / POST (crear con FormData + doc)
│       │   └── [id]/
│       │       ├── route.ts              # PUT / DELETE experiencia laboral
│       │       └── verificar/route.ts    # GET (descargar doc) / POST (aprobar/rechazar)
│       ├── postgrado/
│       │   ├── route.ts                  # GET / POST postgrado
│       │   └── [id]/route.ts             # PUT / DELETE postgrado
│       ├── planes/[id]/route.ts          # (pendiente/vacío)
│       ├── usuarios/
│       │   ├── route.ts                  # GET (listado) / POST (crear usuario)
│       │   └── [id]/route.ts             # PUT / DELETE usuario
│       └── reportes/route.ts             # GET: datos filtrados + estadísticas + exportar Excel
│
├── components/
│   ├── shared/
│   │   ├── AdminLayout.tsx               # Sidebar + topbar para panel de administración
│   │   ├── PublicLayout.tsx              # Header + footer para páginas públicas (server)
│   │   ├── PublicHeader.tsx              # Navbar responsivo con estado de sesión
│   │   ├── PublicFooter.tsx              # Footer institucional con info de contacto
│   │   └── LogoutButton.tsx             # Botón cerrar sesión (client)
│   ├── egresados/
│   │   ├── EgresadoForm.tsx              # Form crear/editar egresado (react-hook-form + zod)
│   │   ├── BuscadorEgresados.tsx         # Filtros avanzados con chips activos
│   │   └── EliminarEgresadoBtn.tsx       # Botón eliminar con confirmación
│   ├── usuarios/
│   │   ├── UsuarioForm.tsx               # Form crear/editar usuario con toggle contraseña
│   │   └── EliminarUsuarioBtn.tsx        # Botón eliminar usuario con confirmación
│   ├── perfil/
│   │   ├── HistorialForm.tsx             # Form experiencia laboral con upload de documento
│   │   ├── MiPerfilHistorial.tsx         # Lista historial con edición inline
│   │   ├── MiPerfilPostgrados.tsx        # Lista postgrados con edición inline
│   │   ├── PostgradoForm.tsx             # Form agregar/editar postgrado
│   │   ├── RegistroInicialForm.tsx       # Form primer registro de egresado nuevo
│   │   └── DirectorioToggle.tsx          # Switch visible/oculto en directorio público
│   ├── reportes/
│   │   └── ReportesClient.tsx            # Filtros + tabla + 4 gráficos Recharts + exportar
│   ├── verificaciones/
│   │   └── VerificacionesClient.tsx      # Cards con aprobar/rechazar y modal de motivo
│   └── directorio/
│       └── DirectorioClient.tsx          # Grid de cards de egresados + búsqueda + paginación
│
└── lib/
    ├── schema.ts                         # Drizzle ORM: tablas, enums, relaciones, tipos exportados
    ├── db.ts                             # Pool de conexión PostgreSQL singleton
    ├── auth.ts                           # JWT (jose), bcrypt, getSession, setSession, clearSession
    ├── utils.ts                          # cn(), fmtDate(), fmtDateLong(), ok(), err()
    ├── validations.ts                    # Schemas Zod para todos los formularios del sistema
    ├── tokens.ts                         # Crear/validar/consumir tokens de verificación en BD
    └── email.ts                          # Nodemailer: plantillas HTML para correos del sistema

scripts/
└── seed.ts                               # Poblar BD con admin + egresados + historial de ejemplo

drizzle/
└── migrations/                           # Archivos SQL generados por drizzle-kit

migration_v7.sql                          # Migración manual: columna mostrar_en_directorio
drizzle.config.ts                         # Config drizzle-kit: schema, dialect, credenciales
tailwind.config.ts                        # Paleta institucional turquesa/marino, fuentes, animaciones